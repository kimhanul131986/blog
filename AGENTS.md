# BLOG

- This repo is independent of AIFTER and GOOBNE.
- Keep the `plan-image-implementation` Nixie worktree separate.
- Do not add API keys or `.env` contents to Git.
- Run the project check command after source changes.
